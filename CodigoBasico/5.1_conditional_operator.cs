using System;

class Conditional
{
    static void condition(string[] args)
    {
        // esto es como el operador ternario

        double temperature = 20;
        String message;

        message = (temperature >= 15) ? "Hace mas de 15" : "Hace menos de 15";


    }
}